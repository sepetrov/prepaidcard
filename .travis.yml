language: go

go:
  - 1.9

before_install:
  - go get github.com/golang/dep/cmd/dep
  - dep ensure -v

script:
  - go test -cover -race -v ./...
  - test -z "$(go vet -x ./... | tee /dev/stderr)"
  - test -z "$(gofmt -d -s . | tee /dev/stderr)"
